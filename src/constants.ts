export enum Screen {
  DASHBOARD = "DASHBOARD",
  MAP = "MAP",
}

export enum TextInput {
  TEXT = "TEXT",
  TEXTAREA = "TEXTAREA",
}

export const fileFormats = {
  geojson: ".geojson,application/geo+json,application/json",
  pdf: "application/pdf",
};

export enum Status {
  DEFAULT = "DEFAULT",
  LOADING = "LOADING",
  LOADED = "LOADED",
}
